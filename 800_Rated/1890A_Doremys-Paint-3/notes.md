array will be considered good,

if all the elements are same

if the elements are of two different types of value (array is filled with any number of num1 and num2)
AND the frequency of those two values must be equal or their difference must be 1.

if array consists two elements