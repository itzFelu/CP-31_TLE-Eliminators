if the arr is already sorted then "YES"

if k = 2, then with the subarray size of two, we can place any element at any position. Hence we can sort it.

if k>2, we still can choose k=2 which favours to sort the list